---
import Container from "./container.astro";
import Logo from "./logo.astro";
import SocialLinks from "./social-links.astro";
import { getLangFromUrl, t, getLocalizedPath } from "@/utils/i18n";

const lang = getLangFromUrl(Astro.url);
const currentYear = new Date().getFullYear().toString();
---

<footer class="my-20 border-t border-gray-200 pt-16">
  <Container>
    <div class="grid md:grid-cols-3 gap-12 mb-12">
      {/* Column 1: Logo & CrownGaming Link */}
      <div>
        <Logo size="medium" />
        <p class="text-gray-600 mt-4 text-sm leading-relaxed">
          {t(lang, lang === "en" ? "hero.subtitle" : "hero.subtitle")}
        </p>
        <p class="mt-6 text-sm">
          <a
            href="https://www.crowngaming.it"
            target="_blank"
            rel="noopener noreferrer"
            class="text-[#005bea] hover:underline font-medium"
          >
            {t(lang, "footer.crownGaming")} →
          </a>
        </p>
      </div>

      {/* Column 2: Quick Links */}
      <div>
        <h3 class="font-bold text-[#333333] mb-4">{t(lang, "footer.quickLinks")}</h3>
        <ul class="space-y-3">
          <li>
            <a href={getLocalizedPath("/products", lang)} class="text-gray-600 hover:text-[#005bea] transition text-sm">
              {t(lang, "nav.products")}
            </a>
          </li>
          <li>
            <a href={getLocalizedPath("/about", lang)} class="text-gray-600 hover:text-[#005bea] transition text-sm">
              {t(lang, "nav.about")}
            </a>
          </li>
          <li>
            <a href={getLocalizedPath("/contact", lang)} class="text-gray-600 hover:text-[#005bea] transition text-sm">
              {t(lang, "nav.contact")}
            </a>
          </li>
        </ul>
      </div>

      {/* Column 3: Social */}
      <div>
        <h3 class="font-bold text-[#333333] mb-4">{t(lang, "footer.followUs")}</h3>
        <SocialLinks variant="default" />
      </div>
    </div>

    {/* Bottom Copyright */}
    <div class="border-t border-gray-200 pt-8 text-center">
      <p class="text-sm text-gray-600">
        {t(lang, "footer.copyright", { year: currentYear })}
      </p>
    </div>
  </Container>
</footer>
